[project]
name = "ppcdata"
version = "1.0.0"
description = "Builds the PathOfPriceCheck game-data bundle"
requires-python = ">=3.11"
dependencies = []          # stdlib only; Node supplies the extractor

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
